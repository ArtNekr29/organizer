<Page x:Class="organizer.RegPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:organizer"
      mc:Ignorable="d" 
      d:DesignHeight="250" d:DesignWidth="500"
      Title="RegPage">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Login"/>
            <TextBox Width="200" Name="loginBox"/>
            <TextBlock Text="Password"/>
            <TextBox Width="200" Name="PasswordBox"/>
            <TextBlock Text="Accept Password"/>
            <TextBox Width="200" Name="PassCheckBox"/>
            <Button Margin="0,5,0,0" Name="registr" Content="Sing Up" Click="registr_Click"/>
        </StackPanel>
    </Grid>
</Page>
